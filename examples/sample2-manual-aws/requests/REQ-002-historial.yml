# NADF-GUIDE
# Propósito: Requerimiento manual REQ-002 — API REST de historial de cálculos.
# Configuración: Ajustar criterios y approver; approval.status debe ser granted para ejecutar.
id: REQ-002-historial
title: API REST de historial
description: Exponer GET /history que devuelva los cálculos registrados en DynamoDB.
endpoint: GET /history
lambda: sample2-history-dev
acceptance_criteria:
  - GET /history responde 200 con lista items
  - Sin tabla configurada responde lista vacía (modo local)
approval:
  status: granted
  approver: product-owner
tests:
  - examples/sample2-manual-aws/tests/test_history.py
